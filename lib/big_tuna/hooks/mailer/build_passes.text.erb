The build for project <%= @project.name %> is still humming along just fine!

Commit that passed with flying colors:
Hash: <%= @build.display_name %>
Author: <%= @build.author %> <<%= @build.email %>>
Message: <%= @build.commit_message %>

Link to build:
<%= build_url(@build) %>
